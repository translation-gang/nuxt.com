{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "node scripts/clean-content-git-cache.mjs && NODE_OPTIONS='--max-old-space-size=4096' pnpm run build",
  "installCommand": "corepack enable && corepack prepare pnpm@10.33.2 --activate && pnpm install",
  "crons": [
    {
      "path": "/api/agent/cleanup",
      "schedule": "0 3 * * *"
    }
  ]
}
